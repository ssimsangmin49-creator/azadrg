<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="google-site-verification" content="bJFihZda-7Swj-l7eu6chIFlKixuGxoJCObmwDs6h_o" />

  <title>Environmentally Friendly Polymer Incineration</title>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    :root {
      --bg: #0f172a;
      --card: #111827;
      --text-main: #f9fafb;
      --text-sub: #cbd5f5;
      --accent: #6366f1;
      --border: #1f2933;
    }

    body {
      font-family: "Georgia", "Times New Roman", serif;
      min-height: 100vh;
      background: var(--bg);
      color: var(--text-main);
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .container {
      max-width: 900px;
      padding: 3rem 3rem;
      background: var(--card);
      border-radius: 14px;
      border: 1px solid var(--border);
      text-align: center;
    }

    h1 {
      font-size: 2.2rem;
      line-height: 1.35;
      margin-bottom: 1rem;
      letter-spacing: -0.02em;
    }

    .subtitle {
      font-size: 1rem;
      font-style: italic;
      color: var(--text-sub);
      margin-bottom: 2.5rem;
      line-height: 1.6;
    }

    .buttons {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .btn {
      padding: 0.8rem 1.6rem;
      border: 1px solid var(--accent);
      color: var(--accent);
      text-decoration: none;
      border-radius: 6px;
      font-weight: 600;
      transition: all 0.2s ease;
    }

    .btn:hover {
      background: var(--accent);
      color: #fff;
    }

    footer {
      margin-top: 2rem;
      font-size: 0.8rem;
      color: #94a3b8;
    }

    @media (min-width: 768px) {
      .buttons {
        flex-direction: row;
        justify-content: center;
      }
    }
  </style>
</head>

<body>

  <main class="container">
    <h1>
      An Environmentally Sustainable Method for the Incineration  
      of Polymer-Based Compounds
    </h1>

    <p class="subtitle">
      A non-commercial and regionally unrestricted technical approach  
      for reducing environmental impact during polymer waste disposal
    </p>

    <div class="buttons">
      <a href="introduction.html" class="btn">Introduction</a>
      <a href="uses.html" class="btn">Recommended Uses</a>
      <a href="contact.html" class="btn">Contact</a>
    </div>

    <footer>
      © 2026 AZAD Reserch Group
    </footer>
  </main>

</body>
</html>
